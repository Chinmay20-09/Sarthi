"""
Natural Language Processor skill — holds a normal conversation like a plain
AI. It never fetches or executes tools; it is the last-resort fallback when
no other skill or tool can handle the request.
"""
